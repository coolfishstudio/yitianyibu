mixin share(id, type, oid)
    .post-share-card-box
        .post-share-card-panel
            .post-share-card
                .post-share-card-default-text
                    span 正
                    span 在
                    span 为
                    span 您
                    span 绘
                    span 制
                    span 中
                    span .
                    span .
                    span .
                .post-share-card-default-bg 分享
                img#shareCard(src='')
            .post-share-text
                p ● 长按保存图片，分享给好友即可；
                p ● 点击"更换主题"按钮，切换样式。
            .post-share-btns
                //- a.post-share-btn 更换主题
                a.post-share-btn#shareCancelBtn 取消分享
            iframe.post-share-card(style='display:none;')
            .post-share-data-temp(style="display:none" data-id=`${id}` data-type=`${type}` data-oid=`${oid}`)
    script.
        $(function() {
            var shareId = $('.post-share-data-temp').attr('data-id');
            var shareType = $('.post-share-data-temp').attr('data-type');
            var shareOid = $('.post-share-data-temp').attr('data-oid');
            $('#' + shareId).click(function () {
                $('.post-share-card-box').show();
                initCardTheme();
            });
            $('#shareCancelBtn').click(function () {
                $('.post-share-card-box').hide();
            });
            $('.post-share-card-box').click(function () {
                $('.post-share-card-box').hide();
            });
            // 更换主题
            var initCardTheme = function () {
                $('#inviteCard').removeAttr('src').hide();
                setTimeout(function() {
                    $('#inviteCard').show();
                    $('.post-share-card').attr('src', '/' + shareType + '/' + shareOid + '/share-card');
                }, 0);
            };

        })
